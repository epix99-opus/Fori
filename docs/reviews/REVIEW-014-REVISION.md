### REVIEW-010 复审结果
- VERDICT: PASS
- REQUIRED_CHANGES 逐条检查：
  1. 为所有功能点补齐统一的输入、处理、输出、异常输出定义，覆盖楼盘字典维护/共享、全主体认证、公证交易、推广分发、个性化推广、片区分层、三方价格赋能、六类业务 Agent 任务 → RESOLVED
     - 证据：`docs/PRD.md` §3.0 已声明统一契约用途（行 222），并逐项列出楼盘字典维护/共享（行 224-228）、全主体认证（行 236-240）、公证交易（行 242-246）、推广分发（行 248-252）、个性化推广（行 254-258）、片区分层（行 260-264）、三方价格赋能（行 266-270）以及六类业务 Agent 任务（行 272-306）的输入、处理、输出、异常输出。
  2. 将“四方共赢/四方分配”模型统一成可核算口径，明确买方、卖方、经纪人、平台各自的成本承担、收益让渡和分配比例；说明公证 5% 与四方模型关系 → RESOLVED
     - 证据：`docs/PRD.md` §5.3 明确四方固定为买方、卖方、经纪人、平台（行 1318-1321），说明公证存证服务 5% 不是第五个交易分配主体、从平台服务费份额中列支或代扣代付（行 1321），并以表格列出四方成本承担、收益让渡/获得、分配比例口径和金额（行 1324-1329）；分配方表将经纪人 80%、平台净服务 15%、公证存证 5% 列为可核算金额（行 1331-1335）。
  3. 删除“非官方爬取”方案，改为官方开放平台、授权发布、用户手动转发或合规合作接口，并补充分发合规边界 → RESOLVED
     - 证据：`docs/PRD.md` §3.0 推广分发处理限定“仅通过官方开放平台、授权发布、用户手动转发或合规合作接口发布”（行 248-252）；§4.1 对接方式表使用开放平台 API、官方合作接口、用户授权发布、手动发布引导和 PWA 分享链接（行 770-779）；分发合规边界明确禁止未授权抓取、逆向接口、模拟登录、绕过平台审核或批量私信骚扰，无官方接口时仅生成素材、分享链接、复制文案和手动发布引导（行 793-795）。
  4. 统一“永久留存”和“用户删除个人信息”的边界，明确不可删除、可删除、匿名化留存数据及用户告知与同意机制 → RESOLVED
     - 证据：`docs/PRD.md` 数据处理表区分公证存证数据不可删除（行 531）、核验必需原始件 30 天内删除原始件并保留脱敏摘要（行 532）、非存证业务记录可删除可识别个人信息并仅保留脱敏摘要（行 533）、统计分析数据仅匿名化留存（行 534）、审计日志最小化加密保存（行 535）；同表列明用户权利和告知/同意机制（行 529-535），并定义删除请求 SLA 和不可删除范围反馈机制（行 537）。

### REVIEW-011 复审结果
- VERDICT: PASS
- REQUIRED_CHANGES 逐条检查：
  1. 统一 PRD、SPEC、ARCHITECTURE 对移动端形态的口径，采用 Next.js 14 PWA + 响应式 Web 并列出原生能力桥接方案，或重写 SPEC/ADR → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` 架构原则 P2 明确产品形态统一为 Next.js 14 PWA + 响应式 Web，并将摄像头、活体识别、推送等能力通过 Web API、第三方 H5 SDK 或服务端回调桥接（行 37）；ADR-001 采纳 Next.js 14 PWA + 响应式 Web，说明移动端和 PC 管理端共享 Web 技术栈（行 2314-2322）。
  2. 重构三层解耦说明，业务 Agent 只能依赖平台内核接口，不直接依赖 `AgentInterface` 或框架适配器，框架适配器由平台内核封装调用 → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` 明确业务 Agent 只依赖平台内核接口，不导入 `AgentInterface`、不持有适配器、不直接调用框架 SDK，框架适配器只在平台内核内部由 `TaskScheduler` 封装调用（行 334）；依赖图展示业务 Agent 依赖 `AgentRuntime` / `TaskScheduler` / `EventBus` 等内核接口，内核再调用 `AgentInterface` 和适配器（行 347-392）；依赖规则列出允许和禁止依赖（行 395-399）；ADR-006 再次固化该决策（行 2379-2386）。
  3. 补齐 ER 图与核心表设计，新增公证机构/公证任务、信用档案/信用事件、价格评估结果/价格因子/评估报告实体及其关系 → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` ER 关系包含 `NOTARY_INSTITUTION`、`NOTARY_TASK`、`CREDIT_PROFILE`、`CREDIT_EVENT`、`PRICE_EVALUATION`、`PRICE_FACTOR`、`PRICE_REPORT` 与交易、用户、门店、房源、小区、存证记录的关系（行 1053-1071）；核心表 DDL 新增 `notary_institutions`、`notary_tasks`、`credit_profiles`、`credit_events`、`price_evaluations`、`price_factors`、`price_reports`（行 1148-1236）。
  4. 对齐交易状态机、DDL CHECK 约束、状态迁移表和 Agent 任务输出，确保 `NOTARY_REJECTED` 等状态在所有层一致 → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` `transactions.status` CHECK 包含 `NOTARY_REJECTED`（行 1140-1144）；交易状态机包含 `NOTARY_PENDING --> NOTARY_REJECTED` 和 `NOTARY_REJECTED --> CANCELLED`（行 1525-1541）；状态迁移表含 `NOTARY_REJECTED` 的 Agent 输出、数据库约束和副作用（行 1544-1552）；文档明确要求 `NOTARY_REJECTED` 在 DDL CHECK、状态迁移表、`VALID_TRANSITIONS`、`NOTARY_REQUEST` / `TRANSACTION_STATE_TRANSITION` 输出 schema 同时包含（行 1559）；`VALID_TRANSITIONS` 代码也包含该状态（行 2021-2033）。
  5. 增加高并发容量参数表和验证计划，包含 Kafka 分区与消费者并发、API 副本与 worker、DB/Redis/ES 参数、HPA 阈值、压测规模、k6/Locust 验证命令和通过标准 → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` 高并发容量表列出 Kafka Topic 分区/RF、Consumer 并发、FastAPI Pod/worker、PostgreSQL 连接池和只读副本、Redis Cluster 内存/连接、Elasticsearch shard/replica、HPA API/Agent/WebSocket 阈值（行 1878-1896）；压测数据规模覆盖城市、小区、楼栋、单套住宅、房源、客源、用户、经纪人、交易和存证记录（行 1898-1902）；k6 命令覆盖楼盘字典、移动端混合流量、匹配 SLA、交易状态机（行 1904-1926）；通过标准覆盖 API、楼盘字典、匹配、Agent 和基础设施指标（行 1928-1934）。原要求中的“k6/Locust”以 k6 命令满足验证命令要求，未提供 Locust 但已有可执行压测路径和通过标准。
  6. 修正 Redis HA 表述，明确采用 Redis Cluster 还是 Sentinel 主从；若使用阿里云 Redis 集群版，则按云产品实际故障转移模型描述 → RESOLVED
     - 证据：`docs/ARCHITECTURE.md` 缓存层明确使用 Redis Cluster（行 137），高可用模型说明生产优先使用阿里云云数据库 Redis 集群版，3 主分片 + 3 从副本，主分片故障由云产品完成副本提升、VIP/代理路由切换和槽位恢复；自建时使用 Redis Cluster 自带故障转移，不混用其他主从高可用模式（行 140）。

### REVIEW-012 复审结果
- VERDICT: PASS
- REQUIRED_CHANGES 逐条检查：
  1. 增加“PRD 模块到页面覆盖矩阵”，列出六大模块、PRD 功能点、对应页面/入口、覆盖状态；对模块六和公证机构端明确“无独立 UI / 后台管理 / API 接入”的产品决策 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` §1.4 提供 PRD 模块到页面覆盖矩阵，列出 PRD 模块、功能点、对应页面/入口、覆盖状态、产品决策（行 158-178）；公证机构端说明不提供独立人工 UI、机构通过 API 接收任务和回传结果、平台运营后台只做接口监控和异常工单（行 172）；模块六说明无独立用户 UI，普通用户不可见技术底座，平台运营后台监控任务队列、重试、熔断和告警，运维监控不纳入移动 PWA 主导航（行 177-178）。
  2. 为 21 个核心页面统一补齐页面规格模板：页面名称、路径、所属模块、页面目的、适用角色、布局、数据展示、交互元素、交互流程、空状态、加载状态、错误状态、权限状态、跳转关系 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` 页面规格验收清单表头完整包含要求的 14 个字段（行 1354-1357），并逐行覆盖页面 01 至页面 21（行 1358-1378）。
  3. 补充登录/注册页、实名认证页、独立价格评估入口页、客源管理页、房源管理页、成交统计页的原型级设计；若不纳入 21 个核心页面，则增加“非核心但必需页面”章节 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` §3.2 为“非核心但必需页面设计”，明确这些页面不替换 21 个核心页面但属于必需原型页面（行 1382-1384）；其中补充登录/注册页（行 1386-1406）、实名认证页（行 1408-1428）、独立价格评估入口页（行 1430-1450）、客源管理页（行 1452-1472）、房源管理页（行 1474-1494）、成交统计页（行 1496-1516），均按页面名称、路径、模块、目的、角色、布局、数据、交互、状态和跳转描述。
  4. 为六条 Mermaid 关键流程增加“流程节点 → 页面/组件 → 角色 → 失败/回退路径”映射表，确保流程图可直接驱动原型连线 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` §4.7 提供关键流程节点映射表，表头为流程节点、页面/组件、角色、失败/回退路径（行 1740-1745），并覆盖买家购房、卖房发布、经纪人入驻认证、楼盘字典共建、房价评估查询、交易 + 公证六个流程（行 1742-1816）。
  5. 扩展组件库，至少补齐 Stepper、Tab、筛选 Bottom Sheet、上传/OCR、地图气泡、图表卡、交易时间线、证书卡、权限提示、空状态、错误状态、离线队列提示等业务组件 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` §2.5 业务组件扩展表覆盖 Stepper 分步表单、Tab / Segmented Control、筛选 Bottom Sheet、上传 / OCR 组件、地图房源气泡、图表卡、交易时间线、证书卡、权限提示、空状态、错误状态、离线队列提示，并列出组成、状态和使用页面（行 383-399）。
  6. 修正移动端手势方向描述，并补充手势冲突规则，包括横向图片轮播与边缘返回、Bottom Sheet 下滑关闭与列表滚动、地图拖动与页面滚动优先级 → RESOLVED
     - 证据：`docs/UI_DESIGN.md` §5.1 将全局返回定义为屏幕左边缘向右滑（行 1826-1829），特定页面手势列出地图单指拖拽、Bottom Sheet 下滑关闭、房源图片区横向滑动等（行 1834-1848）；手势冲突规则表覆盖图片轮播横滑 vs 边缘右滑返回、列表项横滑 vs 边缘右滑返回、Bottom Sheet 下滑关闭 vs Sheet 内列表滚动、地图拖动 vs 页面滚动、地图双指缩放 vs 浏览器缩放、客源卡左右滑 vs 垂直列表滚动，并给出优先级、触发规则和回退行为（行 1862-1871）。
